# Plugin Troubleshooter

A RuneLite plugin that helps you quickly find which plugin is causing a problem using binary search (bisect). When you're experiencing issues, this plugin guides you through a wizard-style workflow that systematically disables plugins to isolate the culprit.
